#include <stdio.h>
#include <stdlib.h>


char MinusculaParaMaiuscula(char letra);




 int main() {
 	
 	char (*x)(char);
 	char m;
 	x = MinusculaParaMaiuscula;
 	m = x('H');
 	
 	printf("%c", m);
	
	return 0;
	}
	
char MinusculaParaMaiuscula(char letra){
	return letra + 32;
}
